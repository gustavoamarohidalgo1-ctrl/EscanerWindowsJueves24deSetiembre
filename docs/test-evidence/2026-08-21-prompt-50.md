# Evidencia Prompt 50 — Documentar, pilotear y aceptar versión 1.0

Fecha: 21 de agosto de 2026 (`America/Lima`).

## Resultado honesto

La documentación de la versión 1.0 quedó completa y verificada contra el código. El piloto cerrado
quedó **especificado pero no ejecutado**, y la matriz de aceptación lo declara así: la versión 1.0
**no se declara lista**.

| Entregable | Estado |
| --- | --- |
| README con arquitectura, setup, compilación, pruebas, migraciones, AAB y recuperación | Completo |
| [`../MANUAL_USUARIO.md`](../MANUAL_USUARIO.md) (16 secciones + preguntas rápidas) | Completo |
| [`../RUNBOOK.md`](../RUNBOOK.md) (respaldo, incidentes, rollback, 10 problemas conocidos) | Completo |
| [`../PILOTO_CERRADO.md`](../PILOTO_CERRADO.md) (guion de 21 pasos, solo datos `[DEMO]`) | Especificado, no ejecutado |
| [`../ACEPTACION_V1.md`](../ACEPTACION_V1.md) (22 CUMPLE / 21 NO CUMPLE / 7 NO APLICA) | Completo |

## Controles ejecutados

| Control | Resultado |
| --- | --- |
| `ruby -c` sobre 8 scripts | 8/8 |
| `bash -n` sobre 9 scripts | 9/9 |
| `ruby scripts/scan-repository-secrets.rb` | sin credenciales de alta confianza |
| `ruby scripts/verify-play-assets.rb` | ficha 12/67/1741/367, assets válidos |
| `bash scripts/test-resolve-spotless-base.sh` | verificado |
| `ruby scripts/test-prepare-ci-artifacts.rb` | verificado |
| `node --test functions/test/firestoreIndexes.test.mjs` | 2/2 |

## Controles imposibles en este entorno

`java -version` responde «Unable to locate a Java Runtime»: Gradle no se pudo invocar. No se ejecutó
`ciStaticAnalysis`, ni los diez verificadores, ni pruebas unitarias, ni Lint, ni Kover, ni
instrumentación, ni empaquetado. Los tests de Functions que dependen del Emulator Suite fallan por
`EPERM`/`UNAVAILABLE` (entorno), no por lógica: `accountDeletion` 3/15, `membership` 0/13,
`postPurchase` 0/26.

Evidencia previa en disco, citada en la matriz y **no producida por esta sesión**: pruebas unitarias
de las seis variantes con 0 fallos (799–848 por variante), Lint con 0 errores y 51 advertencias, Kover
al 89,9 % de líneas en `domain.*`, y 12 pruebas instrumentadas en `Pixel_10a(AVD) - 17` con 0 fallos.

## Correcciones de documentación hechas al verificar

1. El README afirmaba que una reinstalación se recuperaba desde la nube. Falso: el pull es lectura
   réplica y jamás escribe en el libro local. Corregido.
2. El README presentaba la exportación JSON de Ajustes como copia portable verificable. Falso:
   `ExportUserDataUseCase` no está expuesto en ninguna pantalla. Corregido.

## Divergencia registrada

`docs/PRIVACY_DATA_LIFECYCLE.md` describe tres controles en «Ajustes → Privacidad y datos»
—exportar datos, política de retención de fotos e interruptor de respaldo— que no existen en la
interfaz. Queda como problema conocido 1, 2 y 3 del runbook y como NO CUMPLE en la matriz.
