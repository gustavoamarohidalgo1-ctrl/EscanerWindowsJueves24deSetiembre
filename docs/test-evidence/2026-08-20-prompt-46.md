# Evidencia de automatización — Prompt 46

Fecha: 2026-08-20  
Entorno: `Pixel_10a` AVD, Android 17 / API 37, cámara posterior `virtualscene`  
Datos: fixture sintético `DemoInvoiceFixture`; no se usaron datos fiscales ni credenciales reales.

## Resultados

| Gate | Resultado |
| --- | --- |
| Unit tests `localDebug` | 765/765, 0 fallos, 0 omitidos |
| Unit tests `cloudDebug` | 802/802, 0 fallos, 0 omitidos |
| Instrumentación Room/data | 228/228, 0 fallos, 0 omitidos |
| Compose + navegación + E2E demo | 91/91, 0 fallos, 0 omitidos |
| Firebase Auth/Firestore/Functions/Storage | 47/47, 0 fallos, 0 omitidos |
| Lint `localDebug` / `cloudDebug` | `BUILD SUCCESSFUL` |
| Cobertura `koverVerify` | `BUILD SUCCESSFUL` |
| Convenciones de semántica/UI | `BUILD SUCCESSFUL` |

El conjunto instrumentado no-data incluyó 22 clases de UI, navegación y recorrido transversal:
entre ellas `DemoInvoiceEndToEndTest`, `HiltUdfRuntimeTest`, `NavigationRecreationTest`, onboarding
hasta Inventario, selección de fuente, cámara, preview, OCR, cabecera, líneas, matching,
resumen/redondeo, duplicados, historial e inventario. El filtro complementario ejecutó las 29 clases
del paquete data; no hubo clases solapadas ni omitidas entre ambos conjuntos.

El E2E demo comprobó 38 líneas, cierre/reapertura de Room, revisiones y enlaces restaurados, dos
confirmaciones concurrentes, una sola compra/auditoría/outbox, 38 movimientos y saldo por producto
igual a la suma independiente del libro de movimientos.

Se añadieron contratos deterministas para Auth, conectividad, transporte y libro remoto: el modo
avión no consume respuestas programadas, la reconexión las reanuda, el conflicto se conserva y la
sesión vencida puede recuperarse. También se verificó una caída a mitad de lote sin perder ni marcar
como intentada la siguiente operación. El flujo Compras → Nueva compra ahora espera que Room cree el
borrador antes de navegar, bloquea el doble toque, recupera el ID tras recreación y confirma el
borrado durable antes de salir.

## Evidencia preservada por CI

El workflow conserva durante 14 días los XML y reportes HTML de unit tests, lint, cobertura,
migraciones Room y el gate Compose/E2E. La instrumentación usa filtros complementarios
`package=com.facturastock.app.data` y `notPackage=com.facturastock.app.data`, de modo que las pruebas
nuevas no dependen de una allowlist. Los artefactos no incluyen fixtures fiscales externos.

En esta ejecución local se preservaron copias independientes de los XML en
`app/build/prompt46-evidence/data-api37.xml` y `app/build/prompt46-evidence/ui-e2e-api37.xml`.

## Control de teléfono físico

No ejecutado: `adb devices -l` detectó únicamente `emulator-5554` y cero teléfonos físicos. El script
de evidencia pasó `bash -n` y rechazó correctamente usar el emulador: exige exactamente un teléfono
ADB físico. Queda pendiente ejecutar y firmar los diez puntos de
`ANDROID_E2E_DEVICE_CHECKLIST.md`, incluida la factura demo de 38 líneas con cámara real. Por ello,
el criterio de teléfono real de Prompt 46 todavía no se declara cumplido.
